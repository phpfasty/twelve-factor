{var $mainNavigation = is_array($navigation['main'] ?? null) ? $navigation['main'] : []}
<!DOCTYPE html>
<html lang="{$html_lang ?? ($lang ?? 'en')}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{$title}</title>
  <meta name="description" content="{$description ?? ($site['description'] ?? '')}">
  {if !empty($keywords)}<meta name="keywords" content="{$keywords}">{/if}
  <meta name="robots" content="{$robots ?? ($site['seo']['robots'] ?? 'index, follow')}">
  {if !empty($canonical_url)}<link rel="canonical" href="{$canonical_url}">{/if}
  {var $seoBase = $site['seo']['canonical_base'] ?? ''}
  {if $seoBase}
  {var $currentPath = $canonical_url ? str_replace($seoBase, '', $canonical_url) : '/'}
  <link rel="alternate" hreflang="ru" href="{$seoBase}{$currentPath}?lang=ru">
  <link rel="alternate" hreflang="en" href="{$seoBase}{$currentPath}?lang=en">
  <link rel="alternate" hreflang="x-default" href="{$seoBase}{$currentPath}">
  {/if}
  <meta property="og:title" content="{$title}">
  <meta property="og:description" content="{$description ?? ($site['description'] ?? '')}">
  <meta property="og:type" content="{$og_type ?? 'website'}">
  <meta property="og:site_name" content="{$site['name'] ?? ''}">
  <meta property="og:locale" content="{$og_locale ?? str_replace('-', '_', ($html_lang ?? ($lang ?? 'en')))}">
  <meta name="twitter:card" content="summary_large_image">
  <meta property="og:image" content="{$site['images']['og'] ?? ''}">
  <meta property="og:image:alt" content="{$site['ui']['og_image_alt'] ?? ''}">
  {if !empty($canonical_url)}<meta property="og:url" content="{$canonical_url}">{/if}
  {var $personLd = $site['seo']['json_ld_person'] ?? null}
  {if $personLd}
  <script type="application/ld+json">{latte:raw json_encode(['@context' => 'https://schema.org'] + $personLd, JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES)}</script>
  {/if}
  {if isset($post) && is_array($post)}
  {var $articleLd = [
    '@context' => 'https://schema.org',
    '@type' => 'Article',
    'headline' => $post['title'] ?? '',
    'description' => $post['excerpt'] ?? '',
    'datePublished' => $post['date'] ?? '',
    'author' => ['@type' => 'Person', 'name' => $site['name'] ?? ''],
    'publisher' => ['@type' => 'Person', 'name' => $site['name'] ?? '']
  ]}
  <script type="application/ld+json">{latte:raw json_encode($articleLd, JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES)}</script>
  {/if}
  {* Self-hosted fonts (same families/weights as former Google Fonts bundle). Preload critical subsets for LCP / body text. *}
  <link rel="preload" href="/static/fonts/nwpStKy2OAdR1K-IwhWudF-R3w8aZQ.woff2" as="font" type="font/woff2" crossorigin>
  <link rel="preload" href="/static/fonts/u-4e0qyriQwlOrhSvowK_l5UcA6zuSYEqOzpPe3HOZJ5eX1WtLaQwmYiSeqqJ-k.woff2" as="font" type="font/woff2" crossorigin>
  {if ($lang ?? 'en') === 'ru'}
  <link rel="preload" href="/static/fonts/nwpStKy2OAdR1K-IwhWudF-R3wsaZfrc.woff2" as="font" type="font/woff2" crossorigin>
  <link rel="preload" href="/static/fonts/u-4e0qyriQwlOrhSvowK_l5UcA6zuSYEqOzpPe3HOZJ5eX1WtLaQwmYiSequJ-mFqA.woff2" as="font" type="font/woff2" crossorigin>
  {/if}
  <link rel="stylesheet" href="/static/fonts/fonts.css">
  <link rel="stylesheet" href="/static/css/styles.css">
  {if isset($extra_stylesheets) && is_array($extra_stylesheets)}
    {foreach $extra_stylesheets as $extraStylesheet}
      <link rel="stylesheet" href="{$extraStylesheet}">
    {/foreach}
  {/if}
  <link rel="icon" href="data:image/svg+xml;base64," type="image/svg+xml">
</head>
<body>
  {if !($hide_layout ?? false)}
    <header class="mk-nav" role="banner">
      <div class="mk-shell">
        <nav class="mk-nav-inner" aria-label="{$site['ui']['nav_main_aria'] ?? ''}">
          <a href="/" class="mk-logo" aria-label="{$site['ui']['aria_logo_home'] ?? ''}">
            <span class="mk-logo-dot" aria-hidden="true"></span>
            <span>{$site['name'] ?? ''}</span>
          </a>
          <ul class="mk-nav-links" role="list">
            {foreach $mainNavigation as $item}
              <li>
                <a
                  href="{$item['url']}"
                  class="mk-nav-link"
                  data-nav-id="{$item['id']}"
                >{$item['label']}</a>
              </li>
            {/foreach}
          </ul>
          <div style="display:flex;align-items:center;gap:0.5rem;">
            {include elements/lang-toggle.latte}
            <button class="mk-theme-toggle" id="theme-toggle" aria-label="{$site['ui']['theme_toggle_aria'] ?? ''}">
              <svg class="mk-icon" id="icon-sun" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M6.34 17.66l-1.41 1.41M19.07 4.93l-1.41 1.41"/>
              </svg>
              <svg class="mk-icon" id="icon-moon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" style="display:none;">
                <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
              </svg>
            </button>
            <button class="mk-mobile-menu-btn" id="mobile-menu-btn" aria-label="{$site['ui']['mobile_menu_open_aria'] ?? ''}" aria-expanded="false" aria-controls="mobile-nav">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/>
              </svg>
            </button>
          </div>
        </nav>
        <nav id="mobile-nav" class="mk-mobile-nav" aria-label="{$site['ui']['nav_mobile_aria'] ?? ''}">
          {foreach $mainNavigation as $item}
            <a href="{$item['url']}" class="mk-mobile-nav-link" data-nav-id="{$item['id']}">{$item['label']}</a>
          {/foreach}
        </nav>
      </div>
    </header>

    <main id="main-content">{$content|noescape}</main>

    <footer class="mk-footer" role="contentinfo">
      <div class="mk-shell">
        <div style="display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:1rem;">
          <div style="display:flex;align-items:center;gap:0.625rem;">
            <span class="mk-logo-dot" aria-hidden="true"></span>
            <p class="mk-footer-copy">{$site['copyright'] ?? ''}</p>
          </div>
          <nav aria-label="{$site['ui']['nav_footer_aria'] ?? ''}">
            <ul style="list-style:none;margin:0;padding:0;display:flex;gap:1.25rem;flex-wrap:wrap;">
              {foreach $mainNavigation as $item}
                {if $item['id'] !== 'home'}
                  <li>
                    <a href="{$item['url']}" style="font-size:0.8125rem;color:hsl(var(--muted-foreground));">{$item['label']}</a>
                  </li>
                {/if}
              {/foreach}
            </ul>
          </nav>
        </div>
      </div>
    </footer>
  {else}
    {$content|noescape}
  {/if}

  {if !($hide_layout ?? false)}
    <script>
      (function () {
        'use strict';

        function applyTheme(dark) {
          var html = document.documentElement;
          var iconSun = document.getElementById('icon-sun');
          var iconMoon = document.getElementById('icon-moon');

          if (dark) {
            html.classList.add('dark');
            if (iconSun) {
              iconSun.style.display = 'none';
            }
            if (iconMoon) {
              iconMoon.style.display = '';
            }
            return;
          }

          html.classList.remove('dark');
          if (iconSun) {
            iconSun.style.display = '';
          }
          if (iconMoon) {
            iconMoon.style.display = 'none';
          }
        }

        var themeToggle = document.getElementById('theme-toggle');
        var storedTheme = localStorage.getItem('mk-theme');
        var prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        applyTheme(storedTheme ? storedTheme === 'dark' : prefersDark);

        if (themeToggle) {
          themeToggle.addEventListener('click', function () {
            var isDark = document.documentElement.classList.contains('dark');
            applyTheme(!isDark);
            localStorage.setItem('mk-theme', isDark ? 'light' : 'dark');
          });
        }

        var mobileBtn = document.getElementById('mobile-menu-btn');
        var mobileNav = document.getElementById('mobile-nav');
        if (mobileBtn && mobileNav) {
          mobileBtn.addEventListener('click', function () {
            var isOpen = mobileNav.classList.toggle('open');
            mobileBtn.setAttribute('aria-expanded', isOpen ? 'true' : 'false');
          });

          mobileNav.querySelectorAll('a').forEach(function (link) {
            link.addEventListener('click', function () {
              mobileNav.classList.remove('open');
              mobileBtn.setAttribute('aria-expanded', 'false');
            });
          });
        }

        var currentPath = window.location.pathname.replace(/\/$/, '') || '/';
        var navLinks = document.querySelectorAll('[data-nav-id]');
        navLinks.forEach(function (link) {
          var href = link.getAttribute('href') || '';
          var linkPath = href.replace(/\/$/, '') || '/';

          if (linkPath === currentPath) {
            link.classList.add('active');
            link.setAttribute('aria-current', 'page');
            return;
          }

          if (currentPath.startsWith(linkPath) && linkPath !== '/' && linkPath.length > 1) {
            link.classList.add('active');
          }
        });
      })();
    </script>
  {/if}
</body>
</html>
